# Configurações para Docker Swarm Deploy
# Copie este arquivo e configure suas variáveis

# =============================================================================
# DOMÍNIO (Obrigatório)
# =============================================================================
DOMAIN=meusite.com

# =============================================================================
# CHAVES DE SEGURANÇA (Obrigatório - Gere chaves seguras!)
# =============================================================================
JWT_SECRET=sua-chave-jwt-super-segura-de-32-caracteres-ou-mais-aqui
SESSION_SECRET=sua-chave-sessao-super-segura-de-32-caracteres-ou-mais-aqui

# =============================================================================
# TRAEFIK (Opcional - para dashboard)
# =============================================================================
ACME_EMAIL=admin@meusite.com

# Gere com: echo $(htpasswd -nb admin sua-senha) | sed -e s/\\$/\\$\\$/g
TRAEFIK_AUTH=admin:$$2y$$10$$mF/RCxMzNK1HiYGu.XDmBOvMi.HlYn/p2x.IbMsmnqNnMPnc0WOTS

# =============================================================================
# APLICAÇÃO (Já configurado no docker-compose.yml)
# =============================================================================

# PostgreSQL VPS (já configurado)
DB_HOST=148.230.78.128
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=929d54bc0ff22387163f04cfb3b3d0fa
DB_NAME=postgres

# MQTT (já configurado)
MQTT_BROKER_URL=broker.emqx.io
MQTT_PORT=1883

# =============================================================================
# COMO USAR:
# =============================================================================
# 1. Copie este arquivo: cp .env.docker.example .env.docker
# 2. Edite o .env.docker com suas configurações
# 3. Configure as mesmas variáveis no Portainer
# 4. Deploy as stacks do Traefik e LavControl